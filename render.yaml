services:
  - type: web
    name: rag-teaching-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    runtime: python-3.11.9